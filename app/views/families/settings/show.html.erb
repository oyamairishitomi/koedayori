<% content_for(:title, "こえだよりの設定｜こえだより") %>
<div class="w-full">
  <%= render "shared/success_messages", messages: [flash[:notice]].compact %>
    <%= render "shared/page_title", text: "こえだよりの設定" %>
    <%= form_with url: families_settings_path, method: :patch do |form| %>
    <% speaker = current_family.speakers.first %>

<fieldset class="fieldset bg-base-100 border border-base-300 rounded-box p-4">
  <legend class="fieldset-legend">通知</legend>
  <ul class="list bg-base-100 rounded-box">
    <li class="list-row items-center">
      <div>こえが届いたら画面で知らせる</div>
      <div class="justify-self-end"><%= form.check_box :notifications_enabled, class: "toggle sm:ml-auto", checked: speaker&.notifications_enabled %></div>
    </li>
    <li class="list-row items-center">
      <div class="min-w-0">何時までに届かなければ画面上で知らせるか</div>
      <div class="justify-self-end"><%= form.select :notify_at, (0..23).map { |h| format("%02d:00", h) } ,{selected: speaker&.notify_at&.strftime("%H:%M") }, {class: "select sm:ml-auto w-24"} %></div>
    </li>
  </ul>
</fieldset>

<fieldset class="fieldset bg-base-100 border border-base-300 rounded-box p-4">
  <legend class="fieldset-legend flex justify-between w-full">
    <span>こえを届ける人</span>
    <%= link_to "＋ 追加する", new_families_speaker_path %>
  </legend>
  <ul class="list bg-base-100 rounded-box">
    <% current_family.speakers.each do |speaker| %>
    <li class="list-row items-center">
      <div><%= speaker.name %></div>
      <div class="justify-self-end"><%= link_to "吹き込みの準備", families_speaker_path(speaker) %></div>
    </li>
    <% end %>
  </ul>
</fieldset>

<fieldset class="fieldset bg-base-100 border border-base-300 rounded-box p-4">
  <legend class="fieldset-legend">こえを聞く人</legend>
  <ul class="list bg-base-100 rounded-box">
    <li class="list-row items-center">
      <div>あいことばの修正</div>
      <div class="justify-self-end"><%= form.text_field :aikotoba, class: "input" %></div>
    </li>
    <li class="list-row items-center">
      <div>メールアドレスの修正</div>
      <div class="justify-self-end"><%= form.text_field :email, class: "input" %></div>
    </li>
  </ul>
</fieldset>

  <%= form.submit "設定を更新する", class: "btn btn-primary mt-4" %>
  <div class="divider"></div>

<fieldset class="fieldset bg-base-200 border border-base-300 rounded-box p-4">
  <legend class="fieldset-legend">停止設定</legend>
  <ul class="list bg-base-200 rounded-box" >
    <% current_family.speakers.each do |speaker| %>
    <li class="list-row items-center">
      <div><%= speaker.name %></div>
      <div class="justify-self-end"><%= link_to "受け取りを停止する", deactivate_families_speaker_path(speaker), data: { turbo_method: :patch, turbo_confirm: "本当に停止してよろしいですか？二度と戻せません。" }  %></div>
    </li>
    <% end %>
  </ul>
</fieldset>

  <% end %>
  <div class="divider"></div>
  <%= link_to "退会する", new_families_withdrawal_path, class: "text-error" %>
</div>
